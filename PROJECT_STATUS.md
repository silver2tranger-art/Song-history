# 宋代历史可视化网页：项目状态与交接记录

最后更新：2026-09-02

## 1. 当前版本基线

- GitHub 仓库：`https://github.com/silver2tranger-art/Song-history`
- 当前分支：`main`
- 网页版本提交：`854f2b8 优化时间轴、大事记与政权档案`
- 主要页面：`index.html`
- 图片目录：`images/`
- 线上地址：`https://tztzxg.xyz/history/song/`
- 技术形态：纯静态单页，无包管理器、无前端框架、无构建步骤

`index.html` 已提交并推送到 `origin/main`。根目录的 `修改意见V1.docx` 当前未被 Git 跟踪，属于用户参考资料，不应默认上传。

## 2. 产品定位与设计方向

网页面向希望快速理解宋代及同时期东亚政权关系的普通读者。体验不是传统文字年表，而是将时间、地理和政权叙事联动起来的互动式历史长卷。

现有视觉方向：

- 宣纸米色背景与纸张纹理
- 深褐色正文、朱砂红强调色、赭金色辅助线
- 圆角纸张卡片、细边框、克制阴影
- 宋代长卷与档案阅读气质
- 桌面端和手机端统一设计语言

核心设计变量位于 `index.html` 顶部 `:root`，包括 `--paper`、`--paper-deep`、`--ink`、`--cinnabar` 等。后续修改应优先沿用这些变量。

## 3. 当前页面结构

页面目前按以下顺序组织：

1. 首图与导语
2. `01 · 时间总览`
3. `02 · 政权生命周期`
4. `03 · 地理空间`
5. `04 · 大事记`
6. `05 · 政权档案`
7. 资料说明

历史时间范围为 907—1279 年，默认显示 1141 年。全局年份由 `state.currentYear` 管理，并同步驱动年份读数、政权生命周期、地图、大事记和相关跳转。

## 4. 本轮已完成工作

### 4.1 时间控制器 Sticky 稳定性

问题背景：早期版本在慢速滚动、固定与解除的临界点仍会出现回弹或抖动。

当前方案：

- 原生 Sticky 改为“固定面板 + 稳定占位槽”。
- `timeline-slot` 在固定期间保留展开状态的高度，防止下方内容突然上移。
- 时间轴前设置进入与退出两个哨兵，形成约 16 像素的方向滞后区。
- 根据滚动方向决定固定或解除，避免同一边界反复切换。
- 使用 `requestAnimationFrame` 在同一帧更新方向并判断哨兵位置，修复反向慢滑时观察器和方向更新的时序竞态。
- 使用 `ResizeObserver` 和重新测量逻辑维持宽度、高度及响应式布局。

已验证结果：

- 桌面端展开占位高度约 346 像素，固定后控制器高度约 72 像素。
- 390 像素手机端展开高度约 254 像素，固定后高度约 64 像素。
- 慢速向下、向上以及边界附近反复滚动时只发生一次状态切换，占位高度不变。
- 固定状态下年份滑杆仍可拖动，并正确更新其他区域。

### 4.2 大事记五条完整事件窗口

问题背景：旧版只显示当前年份附近的部分事件，滚动可能停留在半条事件处，切换年份或政权后上下文不稳定。

当前方案：

- 筛选后渲染该范围内的完整事件序列。
- 桌面端事件行高 116 像素、间隔 10 像素、窗口高度 620 像素。
- 手机端事件行高 98 像素、间隔 7 像素、窗口高度 518 像素。
- 两种尺寸均同时显示 5 条完整事件。
- 使用纵向强制吸附和逐条滚动逻辑：桌面端单步 126 像素，手机端单步 105 像素。
- 鼠标滚轮、方向键每次移动一条；Page Up／Page Down 每次移动五条。
- 切换当前年份或政权筛选后，查找距离当前年份最近的事件，并默认将其置于窗口第一条。
- 如果后续事件不足四条，则把起点向前移动，以最后五条填满窗口。
- 上下文提示已移到滚动容器外；用户后来要求移除的可见说明“每次对齐一条事件 · 窗口显示5条”已彻底删除，相关无用样式也已清理。

已验证结果：

- 1141 年默认第一条为“绍兴和议”，窗口显示五条完整事件。
- 1279 年附近自动显示最后五条，第一条为 1270 年事件，最后一条为 1279 年事件。
- 高丽筛选能定位最近事件并从其开始显示。
- 大理只有五条记录时，五条全部完整显示。
- 键盘和滚轮滚动后都落在完整行边界，没有半条事件。

### 4.3 政权档案关键节点详情

问题背景：点击“完整关键节点”后的说明过短，缺少事件过程和意义。

当前方案：

- 为八个政权的全部关键节点新增详细数据，共 80 个节点。
- 弹出详情分为“事件概述”和“历史意义”两部分。
- 每个节点两段合计控制在 50—100 个中文字符。
- 保留“查看某年”按钮，可将该节点年份同步到全局时间轴。
- 找不到专用详情时仍保留从事件记录生成内容的回退逻辑。

各政权节点数量：

| 政权 | 节点数 |
| --- | ---: |
| 宋 | 15 |
| 辽（契丹） | 10 |
| 西夏 | 11 |
| 金 | 11 |
| 蒙古／元 | 13 |
| 大理 | 5 |
| 高丽 | 10 |
| 西辽 | 5 |
| 合计 | 80 |

全部 80 个节点的说明长度已经程序化检查，均符合 50—100 字目标。

## 5. 已完成验证

本轮使用本地 HTTP 预览进行了交互和响应式检查：

- 桌面宽度：1366、1920 像素
- 手机宽度：360、390、430 像素
- 所有尺寸均无页面横向溢出
- 所有尺寸的大事记窗口均显示 5 条完整事件
- Sticky 慢速双向滚动和边界反复滚动通过
- 固定状态下拖动年份滑杆通过
- 大事记滚轮、键盘、末尾补足、政权筛选通过
- 八个政权档案节点数量与详情展示通过
- 页面控制台无警告或错误
- `git diff --check` 通过，仅出现 Windows 的 LF／CRLF 提示

## 6. 部署现状

仓库包含 `.github/workflows/deploy-tencent.yml`：

- 当 `main` 分支中的 `index.html`、`images/**` 或部署工作流本身发生变化时，GitHub Actions 会同步网站到腾讯云 COS，并刷新 CDN。
- 仅修改 `AGENTS.md` 或 `PROJECT_STATUS.md` 不会触发网站部署，这是预期行为。
- 除非用户明确要求，不要改变 COS 路径、CDN 地址、Secret 名称或工作流触发条件。

## 7. 本地接续方法

在新电脑上：

```powershell
git clone https://github.com/silver2tranger-art/Song-history.git
cd Song-history
python -m http.server 4173
```

然后访问 `http://127.0.0.1:4173/`。如果 4173 端口被占用，可以换用其他本地端口。

在 Codex 中打开项目后，建议首条消息使用：

> 请先完整阅读 AGENTS.md 和 PROJECT_STATUS.md，检查当前 Git 状态和 index.html，再在保持现有视觉语言与交互规则的基础上继续工作。

## 8. 已知问题与注意事项

- 当前没有已确认的阻塞性问题。
- 时间控制器固定或解除时存在约 0.2 秒的紧凑化过渡，这是设计中的平滑动画，不应表现为页面位置跳动。
- 历史叙述目前以通识展示为目标；若未来用于正式出版或教学，应再做一次系统性的史实、纪年和措辞校订。
- 单文件结构便于静态部署，但数据量继续显著增长时可考虑拆分数据文件；在没有用户批准前不要主动重构。

## 9. 后续建议

按优先级建议：

1. 收集下一轮真实浏览反馈，再处理具体可复现的问题。
2. 对全部历史节点、地图阶段和政权关系进行一次内容校勘。
3. 如需增加事件，保持大事记、政权档案和地图阶段数据一致。
4. 若进行较大视觉改版，先保留当前稳定版本并制作独立预览，不直接覆盖现有设计。
5. 每完成一个阶段版本，同步更新本文件的提交号、完成事项、验证结果和已知问题。
