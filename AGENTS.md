# 宋代历史可视化网页：项目协作说明

## 项目目标

这是一个以 907—1279 年为时间范围的宋朝及同时代东亚历史可视化网页。核心任务是让普通读者通过时间、政权生命周期、地理格局、大事记和政权档案理解宋、辽、西夏、金、蒙古／元、大理、高丽、西辽之间的兴替与互动。

开始工作前必须先阅读根目录的 `PROJECT_STATUS.md`，再检查当前 `index.html` 和 Git 状态。不要仅凭聊天摘要推断现状。

## 技术与文件边界

- 当前网站是纯静态单页：`index.html` 内包含 HTML、CSS、JavaScript 和历史数据。
- 图片资源位于 `images/`，不要随意替换、改名或删除。
- 除非用户明确同意架构升级，否则保持无框架、无构建依赖、可直接静态托管的结构。
- 不要修改 `.github/workflows/deploy-tencent.yml`，除非任务明确涉及部署流程。
- `修改意见V1.docx` 是用户的参考资料，默认不要修改，也不要纳入提交。
- 修改前检查工作区，保留与当前任务无关的用户改动。

## 视觉设计规则

- 保持“宋代历史长卷／纸本档案”的整体气质，不改造成通用科技仪表盘。
- 主要色彩沿用宣纸米色、深褐文字、朱砂红和赭金色；优先复用 `:root` 中的设计变量。
- 保持现有圆角、细边框、纸张纹理、留白和清晰的标题层级。
- 视觉应克制、沉静、适合历史叙事；避免高饱和霓虹色、玻璃拟态和无关装饰动画。
- 桌面端与手机端必须保持同一设计语言，不能只优化单一尺寸。

## 信息结构

按现有顺序维护六个区域：

1. 时间总览
2. 政权生命周期
3. 地理空间
4. 大事记
5. 政权档案
6. 资料说明

除非用户明确要求重构，不改变这些区域的顺序、编号和主要含义。

## 核心交互约束

### 时间控制器

- 年份范围固定为 907—1279 年，默认年份为 1141 年。
- 年份滑杆、关键年份按钮、事件跳转和档案“查看某年”必须使用同一全局年份状态。
- 时间控制器向下滚动后固定在窗口顶部，向上返回原位置时解除固定。
- Sticky 使用稳定占位槽、上下双哨兵、滚动方向和同帧判定形成滞后区；不要退回简单 CSS `position: sticky` 或单一观察器方案。
- 固定与解除时不得导致页面高度变化、位置跳变、回弹或慢速滚动抖动。

### 大事记

- 筛选结果应渲染完整事件序列，不截断为局部事件数组。
- 窗口始终显示 5 条完整事件；每次滚轮或方向键操作移动一整条。
- 使用强制纵向吸附，不能出现半条事件停留在窗口边缘。
- 年份或政权筛选改变后，以距离当前年份最近的事件作为第一条。
- 如果最近事件之后不足 4 条，则向前补足，使窗口仍显示最后 5 条。
- “全部政权”和八个政权筛选按钮必须继续可用。
- 不恢复已经按用户要求删除的“每次对齐一条事件 · 窗口显示5条”可见说明。

### 政权档案

- 保留八个政权标签及完整关键节点。
- 点击关键节点后显示“事件概述”和“历史意义”两个小节。
- 每个节点两部分正文合计以 50—100 个中文字符为目标，既说明发生了什么，也简述其意义。
- 新增或修改历史节点时，同步检查事件年份、政权归属、大事记和档案内容是否一致。

## 内容规则

- 历史文本使用简体中文，语气客观、简洁、面向普通读者。
- 不编造史实；对争议内容避免绝对化表达。
- 政权名称沿用当前写法：宋、辽（契丹）、西夏、金、蒙古／元、大理、高丽、西辽。
- 修改历史数据时优先保持时间线、大事记、地图阶段、政权档案之间的一致性。

## 响应式与无障碍要求

- 至少检查宽度 360、390、430、1366、1920 像素。
- 页面不得出现横向溢出；大事记在桌面和手机端都必须完整显示 5 条。
- 保留语义化标题、按钮、滑杆标签、键盘操作、焦点状态和 `prefers-reduced-motion` 支持。
- 不能通过缩小到难以阅读的字号来解决手机端布局问题。

## 修改与验证流程

- 修改前先理解关联的 CSS、数据和渲染函数，避免只修表象。
- 采用最小、聚焦的改动；不要顺手重写无关区域。
- 页面改动后至少检查：HTML/JavaScript 正常加载、控制台无错误、年份联动正常、Sticky 双向切换、大事记五条完整吸附、筛选定位、档案详情和手机端横向溢出。
- 运行 `git diff --check`，并确认没有把用户参考文件加入暂存区。
- 未经用户明确要求，不自动部署或修改远程基础设施。
- 提交信息用简洁中文说明本次实际改动。

## 交接维护

每完成一个阶段性版本，都要同步更新 `PROJECT_STATUS.md` 中的版本基线、完成事项、验证结果、已知问题和下一步建议，确保另一台电脑或新的 Codex 任务能够直接接续。
